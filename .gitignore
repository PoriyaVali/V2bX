example/config.yml
example/main
example/XrayR
example/XrayR*
example/mytest
example/access.logo
example/error.log
api/chooseparser.go.bak
app/Inboundbuilder/.lego/
app/legocmd/.lego/
.vscode/launch.json
example/.lego
example/cert
./vscode
output/*
.idea/*
newV2bX.sh
quic_singbox.sh

# Runtime artefacts from running a node locally. node/*.key and node/*.pem are
# real private keys and certificates - a local test run drops them here, and
# they must never reach the repo.
node/cert/
node/*.key
node/*.pem
# Knowledge-graph output, regenerated on demand.
graphify-out/
